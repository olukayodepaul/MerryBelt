<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mobile.merrybelt" >

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MerryBelt" >
        <activity android:name=".ui.users.UserConsole"></activity>
        <activity android:name=".ui.profile.SignUpByReferral" />
        <activity android:name=".ui.profile.CompleteSignUp" />
        <activity android:name=".ui.profile.ConfirmSignUp" />
        <activity android:name=".ui.profile.SignIn" />
        <activity android:name=".ui.profile.ResetSignUpPin" />
        <activity android:name=".ui.profile.SignUp" />
        <activity android:name=".ui.profile.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>